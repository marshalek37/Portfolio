{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Patryk Marszalek - Front end - developer",
  "scripts": {
    "build": "gulp build",
    "deploy": "npm run build && rsync -avz --delete dist/ user@server:/path/to/destination"
  },
  "dependencies": {
    "package-name": "version"
  }
}